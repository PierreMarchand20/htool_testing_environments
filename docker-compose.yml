version: '3.4'

services:
    ubuntu_openmpi:
        image: htool_testing_environment:ubuntu_openmpi
        build:
            context: ./Ubuntu
            target: ubuntu_openmpi
        

    ubuntu_mpich:
        image: htool_testing_environment:ubuntu_mpich
        build:
            context: ./Ubuntu
            target: ubuntu_mpich
        

    debian_openmpi:
        image: htool_testing_environment:debian_openmpi
        build:
            context: ./Debian
            target: debian_openmpi
        

    debian_mpich:
        image: htool_testing_environment:debian_mpich
        build:
            context: ./Debian
            target: debian_mpich
        
